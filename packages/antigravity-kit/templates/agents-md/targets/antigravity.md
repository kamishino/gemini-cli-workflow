### Antigravity Runtime

- Primary assistant prompt: `GEMINI.md`.
- Rules source: `.gemini/rules/*.md`.
- Workflow source: `.agent/workflows/*.md`.
- Specialist agents: `.agent/agents/*.md`.
- Persistent context: `.memory/*.md`.

### Operational Notes

- Prefer AGK workflow commands for structured execution (`/develop`, `/review`, `/debug`).
- Use `agk status` for quick checks and `agk doctor` for deep diagnostics.
- Use `agk upgrade` to sync installed templates with AGK package templates.
