#!/bin/sh
# Antigravity Kit - Memory Auto-sync Hook
#
# Automatically prompts to update .memory/ when significant changes are detected
# This hook runs before each commit

node .agent/hooks/sync-memory.js || exit 0
# Don't block commits if sync fails
