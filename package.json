{
  "name": "kamiflow",
  "private": true,
  "description": "Indie Builder's OS - Root Workspace",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "sync": "npm --prefix cli-core run sync",
    "doctor": "npm --prefix cli-core run doctor",
    "update": "npm --prefix cli-core run update"
  }
}
