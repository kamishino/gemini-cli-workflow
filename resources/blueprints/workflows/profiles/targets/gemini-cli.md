### Runtime Profile: Gemini CLI

- Primary command/rule surfaces: `.gemini/commands/` + `.gemini/rules/`.
- Follow Gemini command semantics and avoid assumptions about non-Gemini slash behavior.
- Keep output aligned with transpiled command and rule contracts.
