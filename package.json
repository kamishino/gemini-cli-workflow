{
  "name": "kamiflow",
  "private": true,
  "description": "Indie Builder's OS - Root Workspace",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "sync": "npm --prefix cli-core run sync",
    "transpile": "npm --prefix cli-core run transpile",
    "dev": "npm --prefix cli-core run dev",
    "build": "npm --prefix cli-core run build",
    "sync-all": "npm run dev && npm run build",
    "doctor": "npm --prefix cli-core run doctor",
    "update": "npm --prefix cli-core run update"
  },
  "version": "2.39.0"
}
