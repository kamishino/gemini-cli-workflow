<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KamiFlow Sync Hub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #0f172a; color: #e2e8f0; font-family: 'Inter', system-ui, -apple-system, sans-serif; }
        .container { margin-top: 50px; }
        .card { background-color: #1e293b; border: 1px solid #334155; color: #f8fafc; }
        .table { color: #cbd5e1; }
        .table-hover tbody tr:hover { background-color: #334155; color: #fff; }
        .btn-danger { background-color: #ef4444; border: none; }
        .btn-danger:hover { background-color: #dc2626; }
        .badge-git { background-color: #3b82f6; color: #fff; text-decoration: none; font-size: 0.8rem; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
        .title { color: #38bdf8; font-weight: 700; letter-spacing: -0.025em; }
        .project-id { font-family: monospace; font-size: 0.85rem; color: #94a3b8; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title">🌊 KamiFlow Sync Hub</h1>
            <span class="badge bg-secondary">v1.1.0</span>
        </div>

        <div class="card shadow-lg">
            <div class="card-body p-0">
                <table class="table table-hover mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th class="ps-4">Project Name</th>
                            <th>ID</th>
                            <th>Git Repository</th>
                            <th>Files</th>
                            <th>Size</th>
                            <th>Last Sync</th>
                            <th class="pe-4 text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (projects.length === 0) { %>
                            <tr>
                                <td colspan="7" class="text-center py-5 text-muted">No projects synced yet.</td>
                            </tr>
                        <% } %>
                        <% projects.forEach(project => { %>
                            <tr class="align-middle">
                                <td class="ps-4">
                                    <strong><%= project.name %></strong>
                                </td>
                                <td>
                                    <span class="project-id"><%= project.project_id %></span>
                                </td>
                                <td>
                                    <% if (project.git_repo) { %>
                                        <a href="<%= project.git_repo %>" target="_blank" class="badge badge-git">GitHub</a>
                                    <% } else { %>
                                        <span class="text-muted">N/A</span>
                                    <% } %>
                                </td>
                                <td><%= project.file_count %></td>
                                <td><%= (project.total_size / 1024).toFixed(2) %> KB</td>
                                <td>
                                    <%= new Date(project.last_sync_at).toLocaleString() %>
                                </td>
                                <td class="pe-4 text-end">
                                    <form action="/dashboard/delete/<%= project.project_id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this project? This will wipe all remote data.')">
                                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
        
        <footer class="mt-5 text-center text-muted small">
            Built with ❤️ for the 10x Indie Hacker.
        </footer>
    </div>
</body>
</html>